<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel = "stylesheet" href="/stylesheets/style.css" >
    <title>Document</title>
</head>
<body>
    
    <div class= "w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class = "text-blue-500" href="/">Main Menu</a>
        </div>
        <h1 class = "text-3xl tracking tighter mb-4 bg-transparent">All users</h1>
        <div class = "users flex gap-10 flex-wrap">
            <% if(users.length > 0){ %>
                <% users.forEach(function(user){ %>
                    <div class = "user max-w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class = "w-full h-52 rounded-lg bg-red-500"></div> 
                         <h3 class = "text-xl tracking tighter mt-2"><%= user.name %></h3>
                         <h5><%= user.email %></h5>
                         <div class = "flex gap-3 mt-10">
                             <a class="text-zinc-600" href="/update/<%= user._id %>">Edit User</a>
                             <a class= "text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600 "> No users yet</h3>
            <% } %>
        </div>
    </div>
</body>
</html>