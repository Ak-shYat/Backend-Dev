<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel = "stylesheet" href="/stylesheets/style.css" >
    <title>Document</title>
</head>
<body>
    <div class= "w-full h-full bg-zinc-800 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500"href="/read">User information</a>
        </div>
        <h1 class = "text-3xl tracking tighter mb-4 bg-transparent">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class = "px-5 py-2 border-2 border-gray-300 rounded-md bg-transparent" type="text" name="name" placeholder="Name" value="<%= user.name %>">
            <input class = "px-5 py-2 border-2 border-gray-300 rounded-md bg-transparent" type="text" name="email" placeholder="Email" value="<%= user.email %>">
            <input class = "px-5 py-2 border-2 border-gray-300 rounded-md bg-transparent" type="password" name="password" placeholder="Password" value="<%= user.password %>">
            <input class = "px-5 py-2 border-2 border-gray-300 rounded-md bg-yellow-500" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>